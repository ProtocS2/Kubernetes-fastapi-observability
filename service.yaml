apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: fastapi-app
  labels:
    app: fastapi-app
    release:  monitoring
spec:
  selector:
    app: fastapi-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
